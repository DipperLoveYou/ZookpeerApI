package com.aaa.zk.crud;

/**
 * fileName:
 * description:
 * author:zyq
 * createTime: 15:43
 */
public class ZKJavaDemo {
/*
    public void init() {
    }

    public static void main(String[] args) throws Exception{
        //建立连接
        String conStr="zk1:2181,zk2:2181,zk3:2181";
        try {
            ZooKeeper zooKeeper=new ZooKeeper (conStr, 10000, new Watcher () {
                @Override
                public void process(WatchedEvent watchedEvent) {
                    System.out.println ("操作类型："+watchedEvent.getType ()+",操作节点名称："+watchedEvent.getPath ());
                }
            });
            //获取节点名称
            byte[] data=zooKeeper.getData ("/bbb",false,null);
            System.out.println (new String(data));
            System.out.println ("是否连接："+zooKeeper.getState ().isConnected ());
        } catch (IOException e) {
            e.printStackTrace ();
        }
    }*/

}
